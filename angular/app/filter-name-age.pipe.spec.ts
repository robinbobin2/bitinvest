import { FilterNameAgePipe } from './filter-name-age.pipe';

describe('FilterNameAgePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameAgePipe();
    expect(pipe).toBeTruthy();
  });
});
